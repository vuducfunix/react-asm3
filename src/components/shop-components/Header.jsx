import React from 'react'
import classes from './Header.module.css'

const Header = () => {
  return (
    <header className={classes.header}>
      <span className={classes.big}>SHOP</span>
      <span className={classes.small}>SHOP</span>
    </header> 
  )
}

export default Header